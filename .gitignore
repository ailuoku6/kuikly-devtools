# The repository root ignores *.js, but this package *is* JavaScript.
!*.js
!**/*.js

node_modules/
ui/node_modules/
instrumentor/build/
instrumentor/.gradle/

# Regenerated by `npm run build` (also wired to prepack), so not worth tracking.
gradle/libs/*.jar
ui/dist/
.kuiklyPageTemp/
